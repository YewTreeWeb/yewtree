import Typed from 'typed.js'

export const typed = new Typed('#typed', {
  stringsElement: '#typed-headings',
  typeSpeed: 50,
  backSpeed: 50,
  backDelay: 3000,
  startDelay: 1000,
  smartBackspace: true,
  loop: false
})
